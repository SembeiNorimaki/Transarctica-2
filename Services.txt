#Services
edge_occupancy: Keeps track of which edges are occupied by walls
tile_occupancy: Same for tiles

exploration:
grid
los:
  -bresenham_line(tile_start, tile_end)
  -has_los(tile_start, tile_end)
navigation_graph
  -build_graph()
  -is_walkable_2x2(anchor: Vector2i) -> bool
pathfinding
  -find_path(tile_start, tile_end) -> Array[Vector2i]
road
terrain
  -get_cost(tile: Vector2i) -> float
  -get_elevation(tile: Vector2i) -> int
weapon
  -fire_bullet(bullet_scene: PackedScene, from_tile: Vector2i, to_tile: Vector2i, shooter: Unit) -> void
